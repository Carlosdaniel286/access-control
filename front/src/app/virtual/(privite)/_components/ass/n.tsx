export function N() {
  /*

   const inputArray = value.trim().split('')

    if (inputClear) {
      return setValueInput(inputArray.join(''))
    }

    if (inputArray.length > 10) return

    if (inputArray.length === 3) {
      inputArray.splice(2, 0, '/')
    }

    if (inputArray.length === 6) {
      inputArray.splice(5, 0, '/')
    }

    const dayValidation = {
      firstDigitTooHigh: Number(inputArray[0]) > 3,
      dayOutOfRange: Number(inputArray.join('').slice(0, 2)) > 31,
    }

    const monthValidation = {
      firstDigitTooHigh: Number(inputArray[3]) > 1,
      monthOutOfRange: Number(inputArray.join('').slice(3, 5)) > 12,
    }

    if (dayValidation.firstDigitTooHigh) {
      inputArray.splice(0, 0, '0')
    }

    if (dayValidation.dayOutOfRange) {
      setFormInfo({
        hasError: true,
        message: 'O dia máximo permitido é 31.',
      })
      return
    }

    if (monthValidation.firstDigitTooHigh) {
      inputArray.splice(3, 0, '0')
    }

    if (monthValidation.monthOutOfRange) {
      setFormInfo({
        hasError: true,
        message: 'O mês máximo permitido é 12.',
      })
      return
    }

    setFormInfo(prev => ({ ...prev, hasError: false }))
    setValueInput(inputArray.join(''))
      */
}