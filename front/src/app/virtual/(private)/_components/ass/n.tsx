export function N() {
  /*
 const isLetter = /^[a-zA-Z\s]+$/.test(value.slice(0, 3));
     const isNumber = /^[0-9\s]$/.test(value.slice(3, 4));
     

     console.log(isNumber)
    
     if(!isLetter) return
      */
}